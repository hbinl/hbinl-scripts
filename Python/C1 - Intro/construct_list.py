"""
This program first asks the user how many values to input, and then
adds the subsequent N inputs into a list.

@author Hao Bin
@since 20140730
@modified 20140730
@param none
@precondition: None
@postcondition:  None
@complexity O(1)
"""

size = 3 #int(input("Enter number of values: "))
#Asks the user how many values to input into a list
the_list = []
#initialise an empty list

for i in range(size):
    #Repeat N times, adding the user's input into the list
    #the_list.append(int(input("Value: ")))
    print(1)

#Output the list
print(the_list)
